# Optional: Override VLM provider (default set in config.py)
# VLM_PROVIDER=gemini

# Optional: Override system prompt (default set in config.py)
# SYSTEM_PROMPT="Your custom prompt here"

# Required for Gemini provider
GOOGLE_API_KEY=your_key_here 